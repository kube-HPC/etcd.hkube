const QueueMetrics = require('./queue-metrics/queue-metrics');
const ResourceRequirements = require('./resource-requirements/resource-requirements');
const TemplatesStore = require('./templates-store/templates-store');

module.exports = {
    QueueMetrics,
    ResourceRequirements,
    TemplatesStore
};
